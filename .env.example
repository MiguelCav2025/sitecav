# Site CAV - Centro de Audiovisual de Sao Bernardo do Campo
# Copie este arquivo para .env.local e preencha os valores reais.
# .env.local NUNCA vai para o Git (ver .gitignore).

# --- Supabase (projeto "Cav site") ---
# Dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://ogxibzrrfnykfaueinsl.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Chave secreta - usada apenas nas rotas /api/admin/* (server-side). Nunca expor no client.
SUPABASE_SERVICE_ROLE_KEY=

# --- Resend (formulario de contato: /api/contact) ---
RESEND_API_KEY=
